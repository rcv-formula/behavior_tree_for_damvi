vesc_driver_node 에서 /dev/sensors/vesc 안에서 시리얼 포트 찾으려고 함. 이부분 찾아서 내꺼에 쓰면 될것같고 -> VESC
urg_node_driver 에서 172.16.0.10:10940에 값 받아오려고 함. 이부분 찾아서 쓰면 될듯함. -> LiDAR
imu는 이미 연결되어있으니까 usb에 아마 문제 없을것 같긴 함. (stella_ahrs_node 에서 MW-AHRS ROS Init Fail이라고만 뜨네. 여기서 찾아야할듯)


f1tenth_stack 폴더에서 vesc 폴더가 따로 있음. 여기서 vesc driver 폴더로 가면 vesc 노드 관련된 부분 나옴.
connect 함수를 얘네가 정의해서 했네. 이걸 갖다 쓰는것보다는 연결됐을때 열리는 토픽으로 판단해야할듯.
/sensors/core 노드가 vesc의 상태를 표현하는 노드이므로 이걸 사용하자! -> /sensors/core의 경우 custom msg라서 다른거 쓰자..
토픽으로 판단하면 안됨. 퍼블리쉬를 봐야한다..-> 봐도 문제임.


/scan의 경우는 노드는 열림. 근데 값이 안오는것 뿐. 그러면 얘는 ip주소로 보는게 안전할수도?
f1tenth_stack 폴더의 sensors.yaml가 lidar 관련 내용이네 포트주소까지.
근데 값이 안온다는거 자체가 callback 함수를 부르질 않네. 그러면 callback 을 사용한다는게 곧 메시지가 담겨있다 -> lidar가 켜져있다는 뜻이야.따라서 얘도 그냥 기존처럼 콜백함수일때 mark로 시간 남겨서 시간 넘치는지만 확인하면 될듯.

